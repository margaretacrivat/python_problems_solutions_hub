# Task: Create a set named 'numbers' and add the integers from 1 to 5. Using the clear() method, remove all elements
# from the set.

# Solution:
numbers = {1, 2, 3, 4, 5}
numbers.clear()
print(numbers)
